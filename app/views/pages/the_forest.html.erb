<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>TheForest — One Page Parallax</title>

  <style>
    /* ---- Reset léger & tokens ---- */
    *, *::before, *::after { box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    body { margin: 0; font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, "Noto Sans", "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji"; color: #0f1419; background: #0e1113; }
    :root {
      --bg: #0e1113;
      --ink: #e8f1ee;
      --ink-soft: #c9d6d1;
      --accent: #7be495;    /* vert forêt doux */
      --accent-2: #3d8260;  /* vert profond */
      --panel: #141a1d;
      --muted: #889397;
      --nav-h: 64px;
      --maxw: 1200px;
    }

    /* ---- Barre de nav fixe ---- */
    .nav {
      position: sticky; top: 0; z-index: 1000;
      height: var(--nav-h);
      display: flex; align-items: center; gap: 16px;
      padding: 0 20px;
      background: rgba(20,26,29,0.75);
      backdrop-filter: blur(8px);
      border-bottom: 1px solid #1e2529;
    }
    .brand { font-weight: 700; letter-spacing: .5px; color: var(--ink); }
    .nav a {
      color: var(--ink-soft); text-decoration: none; padding: 8px 12px; border-radius: 10px;
    }
    .nav a:hover { background: #1a2226; color: var(--ink); }

    /* ---- Sections ---- */
    .section {
      min-height: calc(100vh - var(--nav-h));
      display: grid; place-items: center;
      padding: 48px 20px;
      border-bottom: 1px solid #1a2024;
    }
    .wrap { width: 100%; max-width: var(--maxw); margin: 0 auto; }

    /* HOME */
    #home {
      background: radial-gradient(1200px 500px at 50% -20%, #1a2420 0%, #0e1113 60%);
      color: var(--ink);
      text-align: center;
    }
    .hero h1 { font-size: clamp(2rem, 4vw, 4rem); margin: 0 0 12px; }
    .hero p { margin: 0 auto 24px; max-width: 60ch; color: var(--ink-soft); }

    /* ABOUT */
    #about {
      background: linear-gradient(180deg, #0e1113, #10171a 60%, #0e1113);
      color: var(--ink);
    }
    .cards {
      display: grid; gap: 16px;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    }
    .card {
      background: var(--panel); border: 1px solid #1d2428; border-radius: 16px;
      padding: 16px; box-shadow: 0 1px 0 rgba(0,0,0,.4);
    }
    .card h3 { margin: 0 0 8px; font-size: 1.1rem; color: var(--accent); }
    .card p { margin: 0; color: var(--ink-soft); }

    /* CONTACT */
    #contact { color: var(--ink); text-align: center; }
    .contact-panel {
      background: var(--panel); border: 1px solid #1d2428; border-radius: 16px; padding: 28px;
    }
    .pill {
      display: inline-block; padding: 8px 14px; border-radius: 999px;
      background: #13201a; color: var(--accent);
      border: 1px solid #264a38; margin-bottom: 14px;
    }

    /* ---- Bouton Back-to-top ---- */
    .to-top {
      position: fixed; right: 16px; bottom: 16px; z-index: 1001;
      padding: 10px 14px; border-radius: 12px; border: 1px solid #1d2428;
      background: #10171a; color: var(--ink);
      text-decoration: none; opacity: 0; pointer-events: none; transform: translateY(8px);
      transition: opacity .2s ease, transform .2s ease;
    }
    .to-top.show { opacity: 1; pointer-events: auto; transform: translateY(0); }
  </style>
</head>

<body>
  <!-- NAV -->
  <nav class="nav">
    <span class="brand">TheForest</span>
    <a href="#home">Home</a>
    <a href="#about">About</a>
    <a href="#contact">Contact</a>
  </nav>

  <!-- HOME -->
  <section id="home" class="section" aria-label="Accueil">
    <div class="wrap hero">
      <h1>TheForest — One Page créative</h1>
      <p>Base front prête : structure, ancres, scroll fluide, bouton “Home ↑”. Prochaine étape : parallaxe et micro-interactions.</p>
      <a href="#about" class="pill">Découvrir les forêts ↓</a>
    </div>
  </section>

  <!-- ABOUT -->
  <section id="about" class="section" aria-label="À propos des forêts">
    <div class="wrap">
      <h2>À propos des forêts</h2>
      <p style="color:var(--muted);margin:8px 0 22px;">Quelques infos rapides (placeholders — on peaufinera le contenu et le style en étape 3).</p>
      <div class="cards">
        <div class="card">
          <h3>Rôle écologique</h3>
          <p>Les forêts régulent le climat, stockent du carbone et abritent une biodiversité immense.</p>
        </div>
        <div class="card">
          <h3>Esthétique immersive</h3>
          <p>Brumes, canopées, mousses et clairières… un terrain parfait pour une UI parallaxe.</p>
        </div>
        <div class="card">
          <h3>Expérience</h3>
          <p>Notre page simulera la profondeur avec des couches (layers) et du mouvement subtil.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CONTACT -->
  <section id="contact" class="section" aria-label="Contact / Alternance">
    <div class="wrap contact-panel">
      <div class="pill">Recherche d’alternance</div>
      <h2>Front-End / Design — disponible</h2>
      <p style="color:var(--ink-soft); max-width:60ch; margin:8px auto 0;">
        Développeuse Front-End orientée design. Création d’expériences visuelles fortes, parallaxe maîtrisée, responsive, accessibilité soignée.
      </p>
    </div>
  </section>

  <!-- Back to top -->
  <a href="#home" class="to-top" aria-label="Remonter en haut">Home ↑</a>

  <script>
    // Affiche le bouton “Home ↑” après un petit scroll
    (function () {
      const btn = document.querySelector('.to-top');
      const onScroll = () => {
        if (window.scrollY > 180) btn.classList.add('show');
        else btn.classList.remove('show');
      };
      document.addEventListener('scroll', onScroll, { passive: true });
      onScroll(); // init
    })();
  </script>
</body>
</html>
